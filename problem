
 29. [单选题] 在 Pregel 计算模型中，图中的每个顶点会对应⼀个计算单元，下列哪⼀项不 属于计算单元的成员变量？（单选题）
A.顶点值（Vertex value）：顶点对应的 PR 值
B.出射边（Out edge）：只需要表示⼀条边，可以不取值
C.消息（Message）：传递的消息
D.⼊射边（Iut edge）：只需要表示⼀条边，可以不取值
#
30. [单选题] 采⽤ MapReduce 实现 PageRank 的计算过程包括三个阶段，下列哪⼀项是错 的？（单选题）
A.第⼀阶段：解析⽹⻚
B.第⼆阶段：PageRank 分配
C.第三阶段：收敛阶段
D.第⼀阶段：收集⽹⻚
#
01. [判断题] 通常情况下，Hive 以⽂本⽂件存储的表会以回⻋作为其⾏分隔符，在华为 FusionInsight Hive 中，可以指定表数据的输⼊和输出格式处理。（判断题）
A.正确
B.错误